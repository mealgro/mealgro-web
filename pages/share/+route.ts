// Unified share landing: app.mealgro.com/<code>
// Static marketing routes (e.g. /about, /help) take precedence over this
// parameterised route, so only unknown single-segment paths resolve here.
export default "/@code";
